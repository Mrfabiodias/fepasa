{
  "name": "fepasa",
  "version": "1.0.0",
  "description": "",
  "main": "controller.js",
  "scripts": {
    "start": "parcel index.html && npm run copy-assets",
    "build": "parcel build index.html && npm run copy-assets",
    "copy-assets": "cpx 'Images/destination_arrow_black.png' dist/ && cpx 'trains.json' dist/"
  },
  "author": "MrFabioDias",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "cpx": "^1.5.0"
  }
}
